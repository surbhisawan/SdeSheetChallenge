#include <bits/stdc++.h> 
int findDuplicate(vector<int> &arr, int n){
	// Write your code here
    int k=0;
    sort(arr.begin(),arr.end());
    for(int i=0;i<n;i++){
        if(arr[i]==arr[i+1])
        {
            k=arr[i];
        }
    }
    return k;
}
